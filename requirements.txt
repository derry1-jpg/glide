numpy>=1.20.0
scipy>=1.7.0
xarray>=2022.0.0
h5py>=3.0.0
h5netcdf>=1.0.0
zarr>=2.10.0
pyproj>=3.0.0
geopandas>=0.10.0
gdown>=4.0.0

# CuPy is an optional, CUDA-specific dependency (no universal wheel). Install the
# wheel matching your CUDA toolkit, e.g.:  pip install cupy-cuda12x
# or use the packaging extra:  pip install glide[cuda12]   (cuda11 / cuda12 / cuda13)
